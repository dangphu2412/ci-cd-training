{
  "name": "docker",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index",
    "lint": "echo \"test: no test specified\" && exit 0",
    "test": "jest --coverage"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "jest": "^30.2.0"
  }
}
