name: CI workflow

on:
  push:
    branches:
      - main

env:
  NODE_ENV: 22.0.0

jobs:
  echo_helloworld:
    runs-on: ubuntu-latest
    name: Echo Hello world
    steps:
      - name: Echo
        run: 'echo "Node env is: $NODE_ENV"'
